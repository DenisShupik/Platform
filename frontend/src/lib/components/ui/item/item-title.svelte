<script lang="ts">
	import { cn, type WithElementRef } from '$lib/utils.js'
	import type { HTMLAttributes } from 'svelte/elements'

	let {
		ref = $bindable(null),
		class: className,
		children,
		...restProps
	}: WithElementRef<HTMLAttributes<HTMLDivElement>> = $props()
</script>

<div
	bind:this={ref}
	data-slot="item-title"
	class={cn(
		'cn-font-heading flex w-fit items-center gap-2 line-clamp-1 text-sm leading-snug font-medium underline-offset-4',
		className
	)}
	{...restProps}
>
	{@render children?.()}
</div>
